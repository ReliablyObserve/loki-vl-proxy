# Compatibility roadmap

Generated by `conformance/scripts/roadmap.py` from the registry's state. Do not edit by hand: change the state of an item, or prove it, and this follows.

## Where we are

- Endpoints with a test comparing the proxy against Loki: **51%**
- Behaviour and feature items: **0 proven**, 3 partial, 8 gaps, 0 waived


## Next: unproven behaviour

Ranked by which clients depend on it.

| Item | Consumers | Tests | Why |
|---|---|---:|---|
| `data-density-and-chart-quality` — Chart density, zero-fill and high-cardinality behaviour | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `data-probing-and-freshness` — Probes the proxy runs, and their cost and staleness | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `numeric-and-response-formatting` — Timestamps, number formatting and empty shapes | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `parser-error-and-label-collision` — Parser errors, __error__ and _extracted collisions | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `series-limits-and-partial-results` — Series limits: error, or partial result with a warning | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `severity-detected-level-derivation` — detected_level is derived on the read path | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `severity-exposure-surfaces` — Where detected_level and level must appear | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `window-bounds-and-step-alignment` — Range windows, bucket edges and step alignment | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |

## In progress

Proven in part; the rest is named in the item's cases.

| Item | Consumers | Tests | Why |
|---|---|---:|---|
| `drilldown-service-landing` — Logs Drilldown ranks services on the landing page | — | 0 | 1/3 deltas proven. |
| `explore-logs-volume-json` — Grafana Explore renders the logs volume for a `| json` query | — | 0 | 2/5 deltas proven. |
| `service-name-derivation` — service_name follows Loki's discovery order | — | 0 | The derivation matches Loki's 13-entry discover_service_name order and is used by selectors, label filters, grouping, la |
