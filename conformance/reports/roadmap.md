# Compatibility roadmap

Generated by `conformance/scripts/roadmap.py` from the registry's state. Do not edit by hand: change the state of an item, or prove it, and this follows.

## Where we are

- Endpoints with a test comparing the proxy against Loki: **51%**
- Behaviour and feature items: **3 proven**, 7 partial, 5 gaps, 0 waived


## Next: unproven behaviour

Ranked by which clients depend on it.

| Item | Consumers | Tests | Why |
|---|---|---:|---|
| `data-density-and-chart-quality` — Chart density, zero-fill and high-cardinality behaviour | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `data-probing-and-freshness` — Probes the proxy runs, and their cost and staleness | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `numeric-and-response-formatting` — Timestamps, number formatting and empty shapes | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `parser-error-and-label-collision` — Parser errors, __error__ and _extracted collisions | — | 0 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |
| `window-bounds-and-step-alignment` — Range windows, bucket edges and step alignment | — | 1 | Seeded from behaviour we have hit in production use; cases are named but not yet written or wired. |

## In progress

Proven in part; the rest is named in the item's cases.

| Item | Consumers | Tests | Why |
|---|---|---:|---|
| `drilldown-service-landing` — Logs Drilldown ranks services on the landing page | — | 0 | 1/3 deltas proven. |
| `explore-logs-volume-json` — Grafana Explore renders the logs volume for a `| json` query | — | 0 | 2/5 deltas proven. |
| `heavy-metric-fetch-bounds` — Metric evaluation reads bounded work from VictoriaLogs, or refuses early | — | 3 | Proven for grouped counts and byte sums: the two-grid recurrence, the tumbling single grid, the per-grid row limit and t |
| `series-limits-and-partial-results` — Series limits: error, or partial result with a warning | — | 3 | limits/top-n-ranking-not-silent is written and wired: topk and bottomk over grouped counts now rank every series per ste |
| `service-name-derivation` — service_name follows Loki's discovery order | — | 0 | The derivation matches Loki's 13-entry discover_service_name order and is used by selectors, label filters, grouping, la |
| `severity-detected-level-derivation` — detected_level is derived on the read path | — | 4 | detected_level is derived per returned row from what VictoriaLogs stored: a stored detected_level field, then the first  |
| `severity-exposure-surfaces` — Where detected_level and level must appear | — | 3 | Log responses, /tail, /patterns and /detected_fields carry the derived detected_level; categorize-labels puts it and a s |

## Proven

Held by at least one test that declares it.

| Item | Consumers | Tests | Why |
|---|---|---:|---|
| `backend-admission-and-heavy-query-queueing` — Heavy backend work is admitted, queued, then refused like Loki's scheduler | — | 2 | Every case is wired to a test: limiter saturation, fairness, the shared per-request queue budget, metadata staying unque |
| `backend-deadlines-and-cancellation` — Work the client has given up on stops in the backend too | — | 1 | The timeout argument and the cancellation propagation are covered by TestBackendRequests_PassRemainingBudgetAsVLTimeout  |
| `operator-configurable-limits` — Every bound on work is an operator flag, documented from one source | — | 4 | The registry tests in internal/config assert that every limit names a declared flag, that every flag is categorised, tha |
