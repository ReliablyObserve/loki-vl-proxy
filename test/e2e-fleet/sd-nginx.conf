server {
    listen 80;
    server_name _;

    location /peers {
        root /usr/share/nginx/html;
        try_files /peers =404;
        add_header Content-Type application/json;
        add_header Cache-Control "no-store";
    }
}
