version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - unused
    - govet
  settings:
    errcheck:
      exclude-functions:
        - (net/http.ResponseWriter).Write
        - (*encoding/json.Encoder).Encode
        - fmt.Fprintf
        - fmt.Fprintln
        - (*net/http.Request).ParseForm
